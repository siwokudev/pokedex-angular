import { FlavourTextLanguageFilterPipe } from './flavour-text-language-filter.pipe';

describe('FlavourTextLanguageFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FlavourTextLanguageFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
